# 방법 1
SELECT
    USER_ID, PRODUCT_ID
FROM
    ONLINE_SALE 
GROUP BY 
    1, 
    2
HAVING
    COUNT(*) > 1
ORDER BY
    1,
    2 DESC

# 방법 2
SELECT
    USER_ID, PRODUCT_ID
FROM
    ONLINE_SALE 
GROUP BY 
    USER_ID, 
    PRODUCT_ID
HAVING
    COUNT(*) > 1
ORDER BY
    USER_ID,
    PRODUCT_ID DESC